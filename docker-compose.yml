services:
  app:
    build: .
    image: ${IMAGE:-job-pipeline:local}
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./config:/srv/config:ro

